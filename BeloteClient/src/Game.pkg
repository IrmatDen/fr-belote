class Game
{
public:
	enum GameMode
	{
		GM_HOST,
		GM_CLIENT
	};

	struct GameVars
	{
		GameMode		m_GameMode;
		CEGUI::String	m_PlayerName;
	};

	GameVars m_GameVars;

public:
	static Game&	getSingleton(void);
	void			Quit(void);

	void			LoadMenu();
	void			LoadGame();

	ClientSocket&	GetClientSocket();
	void			StartServer();
	void			StopServer();
};
